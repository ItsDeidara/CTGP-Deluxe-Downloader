import { NACP } from '@tootallnate/nacp';
export type AssetSection = {
    offset: number;
    size: number;
};
export type AssetHeader = {
    magic: string;
    version: number;
    iconSection: AssetSection;
    nacpSection: AssetSection;
    romfsSection: AssetSection;
};
export type NRO = {
    data: Blob;
    icon: Blob | null;
    nacp: Blob | null;
    romfs: Blob | null;
};
export declare function isNRO(blob: Blob): Promise<boolean>;
export declare function extractAsset(blob: Blob, offset: number): Promise<Blob | null>;
export declare function extractIcon(blob: Blob): Promise<Blob | null>;
export declare function extractNACP(blob: Blob): Promise<NACP>;
export declare function extractRomFs(blob: Blob): Promise<Blob | null>;
export declare function decode(blob: Blob): Promise<NRO>;
export declare function encode(nro: NRO): Promise<Blob>;
